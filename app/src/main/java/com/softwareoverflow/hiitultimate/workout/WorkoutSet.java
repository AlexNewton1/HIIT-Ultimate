package com.softwareoverflow.hiitultimate.workout;

/**
 *  A WorkoutSet consists of a 'work' period followed by a 'rest' period (AKA a 'rep').
 *  The 'rep' is then  repeated a set number of times before being followed by
 */
public class WorkoutSet {

    private String setName;
    private int workTime, restTime, numReps;
}
