<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        <variable
            name="dto"
            type="com.softwareoverflow.hiit_trainer.ui.view.list_adapter.workout.WorkoutListDomainObject" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/selectOnClick"
        android:layout_width="match_parent"
        android:layout_height="@dimen/half_fab_size"
        tools:background="@drawable/bg_rounded_lozenge"
        android:background="@drawable/bg_rounded_lozenge">

        <CheckBox
            android:id="@+id/savedWorkoutSelect"
            android:layout_width="@dimen/half_fab_size"
            android:layout_height="@dimen/half_fab_size"
            android:button="@drawable/checkbox_circle"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:checked="@{dto.selected}"
            tools:checked="true" />

        <EditText
            android:id="@+id/workoutNameET"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:background="@android:color/transparent"
            android:text="@{dto.dto.name}"
            app:layout_constraintBottom_toBottomOf="@+id/savedWorkoutSelect"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/savedWorkoutSelect"
            app:layout_constraintTop_toTopOf="@+id/savedWorkoutSelect" />

        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:background="@drawable/bg_rounded_lozenge_outline"
            android:visibility="@{dto.selected ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@+id/savedWorkoutSelect"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>